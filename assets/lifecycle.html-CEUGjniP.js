import{_ as a,a as n,b as t,o as l}from"./app-F8Unm_Rt.js";const i={};function o(s,e){return l(),n("div",null,[...e[0]||(e[0]=[t('<h1 id="lifecycle" tabindex="-1"><a class="header-anchor" href="#lifecycle"><span>Lifecycle</span></a></h1><p>The <code>PluginManager</code> is reponsible for the Plugin lifecycle and it will call the lifecycle function on all the services that are registered in the PluginManager&#39;s constructor. Those are the important moments when your plugin can react on a change within the application state.</p><h2 id="lifecycle-events" tabindex="-1"><a class="header-anchor" href="#lifecycle-events"><span>Lifecycle Events</span></a></h2><p>The <code>PluginManager</code> manages the events:</p><ol><li>install</li><li>update</li><li>uninstall</li><li>remove</li></ol><p>For each of these events, there are additionally <code>onBefore</code> and <code>onAfter</code>events (e.g. <code>onBeforeInstall</code>).</p><p>The <code>onBefore</code> happens just before all regular <code>on</code> events are executed. The <code>onAfter</code>happens after all additional changes (e.g. saving of the plugin) were executed. You&#39;ll only need those in rare cases.</p><p>The <code>PluginManager</code> automatically rebuilds the entire ServiceCache, when a plugin was installed, uninstalled or updated. It is not rebuild on remove, as remove can only be triggered when the plugin is uninstalled, and when it&#39;s uninstalled it&#39;s cache is already irelevant for the application.</p><p>So you don&#39;t need to cache the services manually inside the Service itself. The caching happens after the <code>on</code> and before the <code>onAfter</code> events are executed. So if you need to cache your data after an <code>onAfter</code> call, you need to do it manually.</p><p>All lifecycle functions are called with two parameters <code>onLifecycleEvent(Plugin $plugin, PluginManifest $manifest)</code>. The manifest could be deduced from the <code>$plugin</code> but as it is commonly used, it creates less overhead passing it directly.</p><h3 id="install" tabindex="-1"><a class="header-anchor" href="#install"><span>Install</span></a></h3><p>When a plugin is discovered, it&#39;ll be listed in the database, but it&#39;s <code>installed_at</code> timestamp will be <code>null</code>. When installed the plugin&#39;s migrations will be run, it&#39;s script and css files will be published and it will be marked as installed, by setting the <code>installed_at</code> value. Installed plugins cannot be removed.</p><h3 id="update" tabindex="-1"><a class="header-anchor" href="#update"><span>Update</span></a></h3><p>When you get a new version of a plugin, you can upload it, that will result in an automatical update of the existing plugin (an update is an upload of a plugin with the exact same name and a higher version number). You cannot update a plugin with a lower version number. This will result in an error.</p><p>On update all migrations that were not run yet, will be run again.</p><h3 id="uninstall" tabindex="-1"><a class="header-anchor" href="#uninstall"><span>Uninstall</span></a></h3><p>When a plugin is uninstalled the data will not be deleted, the database stays quite the same, but existing functionality will not be available anymore, e.g. when a plugin registered an attribute and you uninstall the plugin, said attribute will be rendered as a default textbox, but all associated data remains in the database. Scripts and CSS files will be unpublished and it&#39;s PHP functionalities will be unregistered.</p><p>When installing it again, all those function should work again without any loss of data.</p><h3 id="remove" tabindex="-1"><a class="header-anchor" href="#remove"><span>Remove</span></a></h3><p>Removing a plugin does result in data loss! The migrations of the plugin will be rolled back, meaning e.g. the created table columns will be deleted. And the entire plugin directory will be removed from the system. Before you remove a plugin, you should be certain of what you are doing.</p>',20)])])}const r=a(i,[["render",o]]),c=JSON.parse('{"path":"/developer/plugins/lifecycle.html","title":"Lifecycle","lang":"en-US","frontmatter":{},"git":{"updatedTime":1779373972000,"contributors":[{"name":"Severino","username":"Severino","email":"severin.opel@hotmail.com","commits":1,"url":"https://github.com/Severino"}],"changelog":[{"hash":"67982da4be7ae5b27df7e809d7066cb933ba2891","time":1779373972000,"email":"severin.opel@hotmail.com","author":"Severino","message":"Added lifecycle page"}]},"filePathRelative":"developer/plugins/lifecycle.md"}');export{r as comp,c as data};
